# ============================================================
# others 环境 - 连接团队共享远程服务器
# 拉项目后只需把 spring.profiles.active 改为 others
# 然后修改下面的 IP 地址为团队服务器地址即可
# ============================================================

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://100.85.88.97/hospital_cloud_brain?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: root
    password: hospital2024
  data:
    redis:
      host: 100.85.88.97
      port: 6379
      password: hospital2024
      database: 0
  rabbitmq:
    host: 100.85.88.97
    port: 5672
    username: admin
    password: admin2024
    virtual-host: /
    listener:
      simple:
        concurrency: 2
        max-concurrency: 8
        prefetch: 10
        default-requeue-rejected: false
        retry:
          enabled: true
          max-attempts: 2
          initial-interval: 1000ms
